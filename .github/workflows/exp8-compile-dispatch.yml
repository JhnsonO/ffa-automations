name: EXP8 — dispatch compile proof

on:
  push:
    branches:
      - diag/zc-exp8-image-alloc-size
    paths:
      - '.github/workflows/exp8-compile-dispatch.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch existing RunPod compile gate
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl --fail-with-body -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/JhnsonO/ffa-automations/actions/workflows/oev-wgpu-hal-lockfile-gate.yml/dispatches \
            -d '{"ref":"diag/zc-exp8-image-alloc-size"}'
          echo 'Dispatched EXP8 compile proof.'
